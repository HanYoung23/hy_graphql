import 'package:flutter/cupertino.dart';

class MapPostDetailScreen extends StatelessWidget {
  const MapPostDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
