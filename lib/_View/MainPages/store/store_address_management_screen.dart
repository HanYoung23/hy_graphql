import 'package:flutter/cupertino.dart';

class StoreAddressManagementScreen extends StatelessWidget {
  const StoreAddressManagementScreen({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
