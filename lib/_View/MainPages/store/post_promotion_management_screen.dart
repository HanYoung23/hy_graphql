import 'package:flutter/cupertino.dart';

class PostPromotionManagementScreen extends StatelessWidget {
  const PostPromotionManagementScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
