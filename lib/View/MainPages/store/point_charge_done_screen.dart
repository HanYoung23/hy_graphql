import 'package:flutter/cupertino.dart';

class PointChargeDoneScreen extends StatelessWidget {
  const PointChargeDoneScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
