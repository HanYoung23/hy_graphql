import 'package:flutter/cupertino.dart';

class PostPromotionUploadScreen extends StatelessWidget {
  const PostPromotionUploadScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
