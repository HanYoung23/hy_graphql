import 'package:flutter/cupertino.dart';

class PostPromotionScreen extends StatelessWidget {
  const PostPromotionScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
