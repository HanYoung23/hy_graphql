import 'package:flutter/cupertino.dart';

class LocationPostDetailScreen extends StatelessWidget {
  const LocationPostDetailScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
