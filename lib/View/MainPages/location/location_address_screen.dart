import 'package:flutter/cupertino.dart';

class LocationAddressScreen extends StatelessWidget {
  const LocationAddressScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
